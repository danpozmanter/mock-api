# Gossamer build output (gos build) and CI/release --out-dir
/target/
/dist/

# Gossamer compiler caches (frontend, IR, link stamps)
/.gos-cache/

# Release staging directories and archives
/mock-api-*/
/mock-api-*.tar.gz
/mock-api-*.zip

# Native binaries dropped at the project root
/mock-api
/mock-api.exe

# Coverage and test reports (gos test --coverage / --format junit)
*.profraw
*.lcov
junit*.xml

# env file
.env
